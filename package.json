{
  "name": "consulta-de-tarjetas",
  "version": "0.1.0",
  "private": true,
  "description": "Consulta de tarjetas: SPA móvil de solo lectura, extractos de guías clínicas de fisioterapia.",
  "scripts": {
    "extraer": "node extraer.js tarjeta_hombro.js",
    "build": "python3 build.py",
    "test": "node test-build.js && node test-fuente.js && node test-contenido.js && node test-contenido-cadera.js && node test-recorrido.js && node test-cabecera.js && node test-urgencia.js && node test-selector.js && node test-app.js",
    "extraer-js": "node extraer-js.js",
    "tarjetas": "mkdir -p salida && for r in hombro cadera cervical lumbar rodilla; do OUT_DIR=salida node tarjeta_$r.js || exit 1; done"
  },
  "devDependencies": {
    "docx": "^9.7.1",
    "jsdom": "^26.0.0"
  }
}
