# CLAUDE.md · Consulta de hombro (SPA)

## Qué es
SPA móvil **de solo lectura** que reproduce la tarjeta de consulta de hombro (extracto de la guía clínica de hombro). La usa un fisioterapeuta en el móvil (Android) durante la visita, con la Ficha de primera visita en papel y Heidi Health grabando en segundo plano. Una tarjeta por región: hombro, lumbar, cervical, cadera, rodilla (tarea 4) y tobillo y pie (añadida después, misma mecánica). Idioma de todo: español.

## Decisiones tomadas (no cambiar sin preguntar)
1. **Solo consulta.** No captura datos de pacientes. Prohibido localStorage, sessionStorage, IndexedDB y cookies. El recorrido del árbol (marcas y fichas abiertas) vive en memoria y se pierde al recargar.
2. **La tarjeta manda.** Todo el texto clínico sale del CONTENIDO (`hombro.data.json`, extraído del docx). No reescribir, resumir ni añadir cifras (S, E, LR, umbrales, puntuaciones). Lo que añade la SPA (agrupación de la tabla de rigidez, pista del congelado, botones del árbol, mapas de nombres) es propio: se marca como tal en pantalla o en el código.
3. **El inicio es un selector de región** (una sola URL; tarea 3 hecha). Dentro de cada región **se entra siempre por el árbol** (Banderas rojas y Bisagra y árbol; las fichas de síndrome se alcanzan desde ahí). No hay lista de síndromes en el inicio de la región. Sin popups; plegables con una sola fila abierta.
4. **Recorrido del árbol:** marcas Hecho / Dudoso / No aplica por nodo, panel «Recorrido» y, al hacer scroll, cápsulas en la cabecera en lugar del título.
5. **Móvil primero:** objetivos táctiles ≥ 44 px, variables CSS con tema claro y oscuro (dark según el sistema), tipografía Atkinson Hyperlegible con fallback, y que aguante el tamaño de letra del sistema al 130 %.
6. **Un solo HTML autocontenido.** Sin librerías externas; solo Google Fonts (con fallback).
7. **Sin colores de alarma:** el borde grueso negro sustituye al rojo (decisión de la tarjeta en papel).
8. **Una sola paleta para todas las regiones:** la verde de hombro (`#1F5F4E` en claro, `#5CC0A3` en oscuro), fija en el CSS. `CONFIG.DARK` no se usa en pantalla.
9. **El nombre de la app vive en una sola constante** (`app.json`). De ahí salen el título de la pestaña, la cabecera del inicio, `package.json` (`name` como slug, `description` empieza por el nombre) y, cuando exista (tarea 5), el manifest. `npm test` falla si se desincronizan (`test-app.js`). Las rutas del manifest y del service worker (tarea 5) deben ser relativas: GitHub Pages puede servir el sitio desde una subruta.

## Archivos
Organizado en carpetas (tarea 7): la raíz solo lleva `app.json`, config de npm y lo que publica GitHub Pages (`index.html`, `manifest.webmanifest`, `service-worker.js`, los iconos, `descargas/`); `src/` es el motor de la SPA; `data/` es contenido y datos por región; `tools/` son los scripts de generación; `test/` son las pruebas. GitHub Pages sigue sirviendo desde `main` / `(root)`: solo se puede mover a una carpeta lo que ningún archivo publicado necesita cargar por URL. `descargas/` cuelga de la raíz pero no está al mismo nivel que los tres generados: un enlace de descarga (`<a href>`) puede apuntar a cualquier ruta del repo, a diferencia de `index.html`, el manifest y el service worker, que si se movieran romperían las rutas relativas entre ellos (decisión 9).

**Raíz**
- `app.json` **la única constante con el nombre de la app** (decisión 9): `{nombre}`. La lee `tools/build.py`
- `index.html`, `manifest.webmanifest`, `service-worker.js` lo que publica GitHub Pages; generados por `tools/build.py` a partir de `src/`. Se comitean (no hay build en CI, ver «Reglas de trabajo»)
- `icon.svg`, `icon-192.png`, `icon-512.png`, `apple-touch-icon.png` iconos de la PWA (tarea 5); estáticos, no los toca `build.py`
- `descargas/` (tarea 8) documentos descargables en `.docx`, enlazados desde la pantalla «Documentos» de la SPA: `ficha_primera_visita.docx`, `guia_rapida.docx` y `formulario_previo_<región>.docx` (las seis). Se comitean (igual que los generados, no hay build en CI): los siete se regeneran con `npm run formularios` salvo `guia_rapida.docx`, que no tiene generador y se sube tal cual

**`src/`** (motor genérico de la SPA, sin texto de ninguna región)
- `plantilla.html` código de la SPA; lleva los marcadores `__APP__` (nombre de la app) y `__DATA__` con `{APP, REGIONES, DATOS}` (ver más abajo). El inicio es el selector de región; dentro de una región, `D` son sus datos planos y `R` lo que se deriva de ellos (`cargarRegion()`). Los colores de acento son fijos en el CSS (una sola paleta para todas las regiones)
- `plantilla-manifest.webmanifest`, `plantilla-sw.js` fuente de `manifest.webmanifest` y `service-worker.js` (tarea 5); llevan los marcadores `__APP__`/`__APP_CORTO__` y `__CACHE__`

**`data/`** (contenido de cada región y lo que sale de él; un par `tarjeta_<región>.js` + `spa_<región>.js` por región, más su `<región>.data.json` generado)
- `<región>.data.json` (las seis: `hombro.data.json`, `cadera.data.json`, `lumbar.data.json`, `cervical.data.json`, `rodilla.data.json`, `tobillo_pie.data.json`) datos de esa región ya montados: CONTENIDO de la tarjeta + campos de `spa_<región>.js`. Se generan, no se editan a mano
- `spa_<región>.js` **los campos específicos de la SPA viven aquí, no en el CONTENIDO de la tarjeta**: enlaces del árbol a las fichas, correspondencia síndrome↔pronóstico, pistas de las fichas, agrupación de la tabla orientativa y corte de la nota. Las seis: `spa_hombro.js`, `spa_cadera.js`, `spa_lumbar.js`, `spa_cervical.js`, `spa_rodilla.js`, `spa_tobillo_pie.js`. No es texto clínico: solo nombra filas que ya están en la tarjeta. `ENLACES[nodo].botones` es la forma general (destinos fijos); `.patrones` es un atajo para cuando el nodo reparte un patrón limpio en una sola línea (hombro, y el nodo 4 de lumbar) — no lo fuerces si no encaja (ver «Lo específico de cadera»); un mismo nodo puede llevar `botones` y `patrones` a la vez (lumbar, nodo 4: botón a la orientativa + los cuatro patrones)
- `tarjeta_hombro.js`, `tarjeta_cadera.js`, `tarjeta_cervical.js`, `tarjeta_lumbar.js`, `tarjeta_rodilla.js`, `tarjeta_tobillo_pie.js`: CONFIG + CONTENIDO de cada región (la fuente de verdad; cada uno llama a `generarTarjeta` al cargarse, importado como `require('../tools/plantilla_tarjetas')`)
- `tarjeta_hombro.docx` referencia en papel; ya no entra en la cadena (de él salía `hombro.data.json` con `extraer.py`, retirado)
- `formulario_hombro.js`, `formulario_cadera.js`, `formulario_cervical.js`, `formulario_lumbar.js`, `formulario_rodilla.js`, `formulario_tobillo_pie.js` (tarea 8) solo la cara 2 (rama de la región) del formulario previo a la primera visita: `CONTENIDO(h)` con los helpers de `plantilla_formularios.js`. La cara 1 (tronco común a las seis) vive en la propia plantilla, no aquí. Cada uno llama a `generarFormulario`, importado como `require('../tools/plantilla_formularios')`
- `ficha_primera_visita.js` (tarea 8) genera `ficha_primera_visita.docx` (bloques 0 a 6 más el *body chart*, página aparte); a diferencia de los formularios, no usa una plantilla compartida — es un script suelto, con sus propios helpers de `docx`. Necesita `body_chart.jpg` en la misma carpeta
- `body_chart.jpg` (tarea 8) silueta para marcar la localización de los síntomas, recortada con el mismo encuadre que usa `ficha_primera_visita.js`; solo la usa ese script, no se sirve por URL

**`tools/`** (scripts de generación; ninguno lo sirve GitHub Pages)
- `datos.js` funde `tarjeta_<región>.js` + `spa_<región>.js` en la forma que consume la SPA; `extraer.js` es su CLI y escribe `<región>.data.json` junto a la tarjeta de origen
- `build.py` lee `app.json`, junta los seis `data/<región>.data.json` en `{APP, REGIONES: [{region, nombre, disponible}], DATOS: {<región>: ...}}` y genera, sin más dependencias que la biblioteca estándar: `index.html` (`src/plantilla.html` + ese JSON), `manifest.webmanifest` (`src/plantilla-manifest.webmanifest` + el nombre de `app.json`) y `service-worker.js` (`src/plantilla-sw.js` + un hash de `index.html` para la caché), los tres en la raíz
- `plantilla_tarjetas.js` generador de los docx de las tarjetas (no tocar salvo en la migración). Depende del paquete npm `docx` (ya declarado en `package.json`). Escribe en la carpeta de la variable `OUT_DIR` (por defecto `/home/claude`)
- `plantilla_formularios.js` (tarea 8) librería compartida del formulario previo: helpers (`section`, `q`, `opts`, `hint`, `writeLine`, `choice`, `fila`...), la cara 1 (idéntica en las seis regiones) y el montaje del documento; cada `data/formulario_<región>.js` solo aporta su `CONTENIDO` de la cara 2. Mismo patrón `OUT_DIR` que `plantilla_tarjetas.js` (por defecto `/home/claude`)
- `extraer-js.js` extrae `{REGION, CONFIG, CONTENIDO}` de un `tarjeta_<región>.js` sin generar el docx: `npm run extraer-js -- data/tarjeta_lumbar.js`. Exporta `leer()`, que usan `datos.js` y `test/test-fuente.js`

**`test/`** (pruebas con jsdom; `npm test` las encadena)
- `test-build.js` `npm test` lo ejecuta primero: regenera `index.html`, `manifest.webmanifest` y `service-worker.js` (`tools/build.py`) y falla si alguno difiere del que había, para no publicar nunca una versión desactualizada
- `test-fuente.js` recorre `REGIONES` (las seis) y comprueba que cada `data/<región>.data.json` coincide celda a celda con su `data/tarjeta_<región>.js`, que está al día y que los campos de `spa_<región>.js` nombran filas que existen (1320 comparaciones, 0 diferencias). La forma difiere solo en lo esperable: celdas de árbol como texto unido con `\n` (en el `.js`, arrays de líneas), las filas con `{ span }` (se comparan por su `.span`), y los pies, que la SPA llama `TITULOS.pieA2` y `PRONOSTICO.pie` y en el `.js` son `TITULOS.pieA`/`pieA2` y `TITULOS.pieB`/`pieC` según `SPLIT_A`/`SPLIT_B`
- `test-pwa.js` manifest válido y coherente con `app.json`, iconos con el tamaño que dicen, y el service worker sin rutas absolutas (decisión 9)
- `test-app.js` comprueba que `package.json` e `index.html` coinciden con `app.json` (decisión 9)
- `test-descargas.js` (tarea 8) la entrada a «Documentos» desde el selector no cuenta como una región más (clase `.doc`, no `.nav`); la pantalla lista los ocho enlaces (ficha, guía rápida, seis formularios) con `download`, rutas relativas y, uno a uno, que el archivo exista de verdad en `descargas/`
- `test-contenido.js` (hombro), `test-contenido-cadera.js`, `test-contenido-lumbar.js`, `test-contenido-cervical.js`, `test-contenido-rodilla.js`, `test-contenido-tobillo_pie.js`, `test-recorrido.js`, `test-cabecera.js`, `test-urgencia.js`, `test-selector.js`, `test-wakelock.js` el resto de pruebas. Una prueba de contenido por región, nunca ampliar la de otra

## Comandos
`npm install` · `npm run extraer` (hombro, desde `data/tarjeta_hombro.js`) · `node tools/extraer.js data/tarjeta_<región>.js` (cualquier otra región) · `npm run extraer-js -- data/tarjeta_<región>.js` (solo lee, sin montar el JSON) · `npm run tarjetas` (regenera los 6 docx en `salida/`) · `npm run formularios` (regenera en `descargas/` la ficha y los 6 formularios previos; `guia_rapida.docx` no tiene generador) · `npm run build` · `npm test`

## Forma del CONTENIDO (según `plantilla_tarjetas.js`)
`URGENCIA?` {titulo, lineas} · `BANDERAS` {titulo, cabecera, filas, nota} · `BISAGRA` {pregunta, ramas, apoyo, nota?} · `ARBOL` {filas: [[nodo, texto]]} · `SINDROMES` {aviso, cabecera, filas, nota} · `ORIENTATIVA?` {titulo, cabecera, filas, nota, bloque?} · `PRONOSTICO` {titulo, cabecera, filas, nota, pie} · `TITULOS`. `datos.js` deja `TITULOS` reducido a lo que usa la SPA (`caraA`, `caraB`, `pieA2`) y pasa el pie de la cara B a `PRONOSTICO.pie`. El `CONFIG` de cada región trae `DARK` (color). `BISAGRA.nota` es la aclaración de que la guía de esa región no nombra «bisagra» explícitamente (a diferencia de hombro, `null`) y qué paso de su árbol se usa como tal; `pArbol()` la pinta bajo el «apoyo», en la misma tarjeta.

## Lo que se ve al leer los cinco `.js` (las regiones NO son copias de hombro)
- **Las cinco tienen `URGENCIA`** (3 a 5 líneas) y `ORIENTATIVA`; hombro no tiene `URGENCIA`.
- **`BANDERAS`:** 8 a 12 filas. Lumbar tiene 2 columnas (sin «ayuda en consulta»); las demás, 3, con cabecera «Sospecha → derivación médica».
- **`ARBOL`:** los nodos cambian por región. Hombro 1 2 2b 3 4 · cadera 1 2 3 4 5 5b 6 · cervical 1 2 3 3b 4 5 5b 6 · lumbar 1 2 3 3b 4 · rodilla 1 a 7 · tobillo y pie 1 a 8. El texto de cada nodo es un **array de líneas** (en el JSON de hombro, extraído del docx, viene unido con `\n`). El panel «Recorrido» ya es genérico; las acciones por nodo, no.
- **`SINDROMES`:** 6 (cervical), 7 (lumbar), 9 (cadera), 13 (rodilla), 21 (tobillo y pie, la más grande). Algunas filas tienen 3 celdas con `{ span: "texto" }` en la tercera (síndromes sin ① ni ②, p. ej. miofascial en lumbar, sensibilización central en cadera, rotura del Aquiles/Lisfranc/fracturas del pie en tobillo y pie). La cabecera de esa tabla no está en el CONTENIDO: la fija la plantilla («Síndrome · Explorar · 10′ · ① Gesto testigo · ② Medida objetiva»). `SINDROMES.nota` es opcional: tobillo y pie no la trae (solo `aviso`), a diferencia de las otras cinco.
- **`ORIENTATIVA` es distinta en cada región.** Matriz comparativa en cervical (migraña / tensional / cervicogénica) y lumbar (discogénico / facetario / sacroilíaco); fichas de entidades adicionales en cadera y rodilla (4 columnas, la última con ① y ②); en tobillo y pie, 15 entidades con la misma forma (y una con `{ span }`, Gota). Hay que renderizarla de forma genérica (cada fila con sus columnas etiquetadas por `cabecera`), no con la pantalla de rigidez de hombro. `columnas()` (motor) trata el `{ span }` igual que `pSindrome()`: una sola nota combinada, no una celda más (cambio real al añadir tobillo y pie — antes solo lo hacía `pSindrome()`, y `pOrientativa()` habría roto con la fila de Gota).
- **`PRONOSTICO`:** 6 a 17 filas. En rodilla (13 + 8 entidades frente a 17 filas) y en tobillo y pie (21 síndromes frente a 17 filas) la correspondencia síndrome ↔ pronóstico no es 1 a 1: necesita mapa explícito.
- **`CONFIG.DARK`:** color propio por región (hombro `1F5F4E`, cadera `6A4A6A`, cervical `4A5A7A`, lumbar `1F4E5F`, rodilla `7A5A2E`, tobillo y pie `3E6A2E`). Solo afecta al papel: la SPA usa una sola paleta para todas las regiones. `SPLIT_A` / `SPLIT_B` / `SZ_*` tampoco afectan a la SPA.
- **`TITULOS`:** claves distintas según región (`caraA`, `caraA2`, `caraB`, `caraC`, `pieA`, `pieA2`, `pieB`, `pieC`).
- **El nombre de región no siempre es una sola palabra:** `REGION` puede llevar `_` (`tobillo_pie`); `tools/datos.js` lo convierte a «Tobillo y pie» para pantalla (`nombreRegion()`, sustituye `_` por ` y ` antes de capitalizar). Las demás regiones, de una sola palabra, no lo notan.

## Lo específico de hombro (ya está en datos, en `spa_hombro.js`)
`ENLACES` (botones de los nodos 1, 2b y 4, y el mapa etiqueta del árbol → ficha que antes era `PATRON_A_SINDROME`), `PRONOSTICO_DE` (antes `SINDROME_A_PRON`), `FICHAS` (pista del congelado, botón al diferencial y nota de síndromes), `ORIENTATIVA_GRUPOS` (antes `GRUPO_LIMITADA` / `GRUPO_LIBRE` / `DICE_PASIVA`, con su aviso de agrupación propia) y `CORTE_NOTA`. El nombre de la región sale de la propia tarjeta (`REGION`); el color es fijo (decisión 8), no viene de `CONFIG.DARK`.
El motor lee un nodo con `patrones` partiendo su primera línea en «condición → ETIQUETA» (la condición, literal de la tarjeta, es el subtítulo del botón) y usa el resto de líneas como nota de cierre del árbol; la pantalla `orientativa` pinta cada fila con sus columnas etiquetadas por `cabecera` y la agrupación es una capa opcional.

## Lo específico de cadera (ya está en datos, en `spa_cadera.js`)
El árbol de cadera no reparte un patrón limpio en una sola línea como el de hombro: el nodo 6 mezcla, en dos líneas, síndromes (SDTM, Neuropatías, Sensibilización central) y entidades que solo existen en `ORIENTATIVA` (Aductor, Psoas ilíaco, Inguinal, Púbico: «entidades de Doha»). En vez de forzar `patrones`, `ENLACES` usa solo `botones` (destinos fijos), incluido un grupo propio en el nodo 5b: la tarjeta dice «identificar entidad» sin nombrarlas, así que se listan las cinco filas de SINDROMES cuyo propio texto habla de cribado intraarticular (FAIS, Labrum, Ligamento redondo e inestabilidad, Condropatía, Artrosis).
`PRONOSTICO_DE` puede mapear una síndrome a `null`: sensibilización central no tiene fila de pronóstico en la tarjeta (no es un despiste; la ficha simplemente no muestra el plegable de bloque 6). `SINDROMES.nota` es aquí un aviso general, no de una sola síndrome como en hombro, así que las nueve fichas llevan `nota: true`.
Motor: `pSindrome()` ahora trata las filas con `{ span: "texto" }` en la última celda (síndromes sin ① ni ②, como sensibilización central): una sola sección combinada en vez de dos.

## Lo específico de lumbar (ya está en datos, en `spa_lumbar.js`)
Mezcla los dos mecanismos: el nodo 4 reparte un patrón limpio en una sola línea, como el nodo 4 de hombro (`patrones`; el árbol lo recorta a la pregunta, igual que en hombro), y además lleva un botón fijo (`botones`) a la tabla orientativa, que es precisamente la matriz comparativa de esos mismos patrones — un nodo puede llevar las dos cosas. El nodo 3b, en cambio, tiene dos líneas con tres destinos distintos: como el nodo 6 de cadera, usa solo `botones`.
`ORIENTATIVA` aquí es una matriz comparativa (filas = criterios como «Localización» o «Perfil», no entidades; la primera cabecera va vacía, columnas = los tres síndromes con su prevalencia) en vez de una lista de entidades como en cadera — la misma pantalla genérica sirve para las dos formas, sin cambios de motor.
`SINDROMES.nota` aquí es específica de una síndrome (sacroilíaca, tests de disfunción de movimiento SI), como en hombro, no general como en cadera: solo esa ficha lleva `nota: true`.
`PRONOSTICO_DE` mapea miofascial a `null`: la tarjeta tiene una fila «Curso general» sin dueño explícito, y no está dicho que sea la suya, así que no se le asigna por inferencia.

## Lo específico de cervical (ya está en datos, en `spa_cervical.js`)
Ningún nodo tiene el patrón limpio de una línea de hombro o del nodo 4 de lumbar: los cuatro nodos con destino van todos por `botones` fijos, como el nodo 6 de cadera — `NODO_PATRONES` es `null` aquí, así que todas las fichas llevan su `pista` a mano (no hay `CLAVE_PATRON` del que derivarla). Dos casos a tener en cuenta al leer el árbol de una región nueva:
  - El nodo 5b reparte a dos fichas y, además, a la tabla `ORIENTATIVA` (para «cefalea primaria»: migraña o tensional no tienen ficha propia, la orientativa ES su diferencial) — y menciona «VESTIBULAR», que no tiene ficha ni pantalla en esta SPA: se queda solo en el texto del nodo, sin botón.
  - El nodo 6 reparte tres sub-ubicaciones (craneocervical, faceta, cervical baja), pero las tres son la MISMA ficha (Idiopático ya las distingue en su propio texto): un solo botón, no tres — no asumas que cada etiqueta en mayúsculas del árbol es una ficha distinta.
`SINDROMES.nota` aquí es específica de separar dolor referido de radicular: se muestra en dos fichas (Dolor radicular y Radiculopatía), ni en una sola (como hombro/lumbar) ni en las seis (como cadera). `PRONOSTICO_DE` solo necesita una entrada (Latigazo (WAD) → Latigazo): las otras cinco síndromes coinciden de nombre con su fila de pronóstico.

## Lo específico de rodilla (ya está en datos, en `spa_rodilla.js`)
La más grande y la que más se aleja de hombro. `ORIENTATIVA` aquí no es ni matriz comparativa (lumbar/cervical) ni lista de entidades sin ①② (cadera): es una **segunda tabla de fichas completa**, con la misma forma que `SINDROMES` (13 síndromes + 8 entidades = 21 fichas en total). Ningún nodo tiene el patrón limpio: los nodos 4 y 7 reparten en dos líneas, y el nodo 7 encima usa otra gramática («LOCALIZACIÓN → entidad1 (detalle) · entidad2 (detalle)», en minúsculas, sin «X → ETIQUETA»), que ni intentaría parsear `patrones`. Todo va por `botones`; un mismo síndrome (Menisco, LCP) puede tener un botón en más de un nodo (agudo y crónico) — son destinos repetidos, no un problema.
`PRONOSTICO_DE` es aquí el mapa más complejo: 17 filas de pronóstico para 13 síndromes, con una fila combinada (LCP → «LCP · LLE · EPL», el horizonte general de esos tres) y una fila específica que sí coincide de nombre (LLE y EPL → LLE y EPL); Lesión osteocondral, `null` (sin fila). Las entidades de `ORIENTATIVA` no reciben pronóstico propio en esta SPA (ninguna región lo hace: `pOrientativa()` no lo muestra) — algunas de sus filas sí tienen fila de pronóstico en la tarjeta (Artrosis, Cintilla iliotibial...) y se quedan sin mostrar en ninguna pantalla, igual que «Curso general» en lumbar.
`ORIENTATIVA_GRUPOS` sí tiene grupos con nombre aquí (Medial, Lateral, Posterior): el propio nodo 7 organiza las ocho entidades por localización, así que no es una agrupación inventada.

## Lo específico de tobillo y pie (ya está en datos, en `spa_tobillo_pie.js`)
`REGION = 'tobillo_pie'` (con `_`, no una sola palabra: ver «Lo que se ve al leer...»). La región más densa de todas: 21 síndromes + 15 entidades de `ORIENTATIVA` = 36 fichas, y el único nodo (7) con 14 botones en una sola pantalla.
Ningún nodo tiene el patrón limpio de una línea, aunque dos lo parecen a primera vista: el nodo 4 tiene un «→» anidado dentro del propio paréntesis explicativo de uno de sus destinos (rompe el split de `patrones`, que solo espera un «→» por segmento), y el nodo 5 reparte un destino a **dos** fichas posibles unidas por «o» en vez de «·» (patrones espera una sola etiqueta por segmento). Ninguno de los dos usa `patrones`; todo va por `botones`, como cadera/cervical/rodilla.
El nodo 5 (patrones pediátricos: Sever, Iselin, Köhler, Freiberg, apofisitis del tibial posterior) casi no tiene botones: ninguna de esas entidades tiene ficha propia en esta tarjeta, así que se quedan en el texto del nodo sin enlace (como VESTIBULAR en cervical) — solo dos sí tienen destino real (osteocondritis disecante y osteoma osteoide) y llevan botón.
El nodo 8 reparte muchísimos destinos de `ORIENTATIVA` (talón plantar / mediopié / antepié / dolor vago del tobillo): en vez de un botón por fila (habría sido más de 15 solo ahí), se consolidan en un botón por grupo con el `sub` nombrando el grupo — los síndromes propios que también menciona (Lisfranc, Sindesmosis, fracturas, estrés) sí llevan botón aparte, uno por ficha.
Esta tarjeta no tiene `SINDROMES.nota` (a diferencia de las otras cinco, que sí la traen): solo `aviso`, con el glosario de siglas (ETM, KTW, LPAA, LPC, LTPAI) que se muestra una vez arriba del árbol. Ninguna ficha lleva `nota: true`.
`PRONOSTICO_DE` es, junto con el de rodilla, el mapa más complejo: 17 filas para 21 síndromes, con cuatro filas combinadas (Aquiles roto/Lisfranc/fracturas; Aquiles media/insercional/vaina/plantar; nervios sural/túnel del tarso/talón; seno del tarso/sinovitis/pinzamiento anterior) y tres síndromes sin fila (`null` explícito: Luxación del tibial posterior, Bursa calcánea superficial, Referido lumbar — la tarjeta no les da pronóstico).
`ORIENTATIVA_GRUPOS` tiene 4 grupos con nombre (Talón plantar, Mediopié, Antepié, Dolor vago del tobillo), sin `resto`: cubren las 15 filas exactas. El propio nodo 8 los organiza así (localización primero, «dolor vago» como catch-all si no encaja ninguna), no es una agrupación inventada — mismo patrón que Medial/Lateral/Posterior en rodilla.
Motor: `columnas()` (usada por `pOrientativa()`) ahora trata las filas con `{ span }` en la última celda igual que `pSindrome()` — cambio real al añadir esta región (Gota, en `ORIENTATIVA`, no muestra ①②).

## Tareas, por orden
1. ~~**Motor común + datos por región.**~~ **Hecho con hombro.** La fuente es `tarjeta_hombro.js` (+ `spa_hombro.js`), `extraer.py` retirado y el motor sin literales de la región. Los 5 docx se regeneran idénticos (comprobado comparando `word/document.xml` antes y después). Queda, al añadir cada región: darle su `spa_<región>.js` y meterla en `REGIONES` de `test-fuente.js`.
2. ~~**URGENCIA.**~~ **Hecho.** Componente genérico en `plantilla.html` (`urgencia()`, clase CSS `.card.urgencia`): lo primero que se ve al entrar en la región cuando `DATA.URGENCIA` existe, con el mismo criterio visual que la tarjeta (borde grueso con `var(--ink)`, sin rojo) y sus líneas literales, sin resumir. Probado con una región sintética en `test-urgencia.js`, y ya con datos reales en cadera.
3. ~~**Selector de región en el inicio.**~~ **Hecho.** `pSelector()` en `plantilla.html`, con `DATA.REGIONES` (las cinco, generadas por `build.py`): las cinco abren. El nombre de la región se ve siempre en la cabecera (`#regionCab`) salvo en el propio selector y en el inicio de la región (ahí ya es el título); también con las cápsulas del recorrido. Probado en `test-selector.js` y en `test-cabecera.js`.
   **Orden de las seis (tras añadir tobillo y pie): Cervical, Lumbar, Hombro, Cadera, Rodilla, Tobillo y pie** — orden fijo, decisión propia (lista `REGIONES` de `tools/build.py`), sin criterio clínico detrás (no por prevalencia: no hay fuente fiable que citar sin inventar cifras, decisión 2). Se valoró y descartó que fuera arrastrable con el orden guardado en el navegador: chocaría con la decisión 1 (prohibido localStorage/sessionStorage/IndexedDB/cookies).
4. ~~**Añadir regiones una a una.**~~ **Hecho.** Las cinco regiones de la ficha tienen `spa_<región>.js`, `<región>.data.json`, están en `REGIONES` de `test-fuente.js` (927 comparaciones, 0 diferencias) y tienen su prueba de contenido propia (`test-contenido-cadera.js`, `-lumbar.js`, `-cervical.js`, `-rodilla.js`), con **todas** las fichas de síndrome y la tabla orientativa alcanzables desde el árbol (decisión 3). El motor (`plantilla.html`) no cambió al añadir lumbar, cervical ni rodilla: solo tuvo un cambio real, al añadir cadera (`{ span }` en `pSindrome()`). Los 5 docx siguen generándose idénticos byte a byte al `word/document.xml` de antes de la tarea 1.
   **Añadida después, misma mecánica: tobillo y pie** (sexta región, fuera de la ficha original). La región más densa (21 síndromes + 15 de `ORIENTATIVA`); ver «Lo específico de tobillo y pie». Sí tuvo un cambio real de motor: `columnas()` (usada por `pOrientativa()`) no trataba `{ span }` — rompía con la fila de Gota — así que se le añadió el mismo tratamiento que ya tenía `pSindrome()`. También `tools/datos.js` gana `nombreRegion()` para regiones de más de una palabra (`tobillo_pie` → «Tobillo y pie»; las demás, de una sola palabra, no lo notan). Los 6 docx (los 5 de antes + tobillo y pie) se comprobaron byte a byte contra `word/document.xml`: los 5 idénticos, tobillo y pie se genera sin errores. `REGIONES` de `tools/build.py` y `test/test-fuente.js` (ahora 1320 comparaciones, 0 diferencias), y `test-contenido-tobillo_pie.js` nuevo.
5. ~~**PWA.**~~ **Hecho.** `manifest.webmanifest` y `service-worker.js`, generados por `build.py` desde `plantilla-manifest.webmanifest` y `plantilla-sw.js` (mismo patrón que `plantilla.html` → `index.html`). El manifest lee el nombre de `app.json` (decisión 9); `short_name` es su primera palabra. El service worker cachea el cascarón (HTML, manifest, iconos) con una caché nombrada con un hash de `index.html`, así que cada build que cambie contenido la invalida sola, sin tocar el archivo a mano; Google Fonts no se cachea (hay *fallback* del sistema, decisión 5). Todas las rutas, en el manifest y en el service worker, relativas (sin `/` inicial). Verificado de verdad con Playwright (no con jsdom, que no implementa Service Worker): recarga sin conexión, navegación dentro de una región, marcar el recorrido y abrir una ficha, todo sin red y sin errores — el service worker controlaba la página. `icon.svg`, `icon-192.png`, `icon-512.png` y `apple-touch-icon.png`: mismo diseño (tarjeta blanca sobre el acento), generados una vez, no se regeneran en cada build.
6. ~~**Wake Lock** (mantener la pantalla encendida): falló dentro del visor de claude.ai; reintentarlo como página propia.~~ **Hecho.** Botón redondo `#pantalla` al final de la cabecera (glifo `◯`/`◉`, sin emoji ni color de alarma), oculto por defecto y visible solo si `'wakeLock' in navigator` (mismo patrón de detección que el service worker de la tarea 5). Es un interruptor manual — no se activa solo al entrar en una región — y, como el recorrido del árbol, vive solo en memoria: se pierde al recargar (decisión 1). Si el navegador suelta el wake lock solo (al ocultar la pestaña), se vuelve a pedir en `visibilitychange` si se seguía queriendo. Probado con `navigator.wakeLock` simulado en `test-wakelock.js` (jsdom no lo implementa) y, de verdad, con Playwright contra la página servida como sitio propio (no en el visor de claude.ai): `navigator.wakeLock.request('screen')` funciona y el botón alterna sin errores — confirma que el fallo original era del visor, no de la SPA.
   **Aviso del estado (después, a raíz de que el glifo solo no bastaba a primera vista):** se descartó un banner o popup al pulsar — el propio icono ya distingue el estado a la vista (relleno + fondo de color) y es persistente, así que sería redundante la mayoría de las veces; y en el único caso donde sí ayudaría (el navegador suelta el wake lock solo, al ocultar la pestaña) tampoco serviría, porque nadie ve la pantalla en ese momento. También se descartó `title` (tooltip): depende de hover, que no existe en móvil (decisión 5 — es el dispositivo real de uso). Lo que sí se hizo: `aria-label` del botón pasa a decir el estado («Mantener la pantalla encendida: activado»/«desactivado»), no solo `aria-pressed` — ayuda de verdad a quien use lector de pantalla, sin depender de hover. `test-wakelock.js` comprueba el `aria-label` en los dos estados.
7. ~~Organizar en carpetas (`src/`, `data/`, `tools/`, `test/`) ajustando rutas.~~ **Hecho.** Ver «Archivos» arriba para qué va en cada carpeta. La raíz solo lleva `app.json`, la config de npm y los tres archivos que publica GitHub Pages (`index.html`, `manifest.webmanifest`, `service-worker.js`) más sus iconos, porque Pages sirve desde `main` / `(root)` sin build en CI (ver «Reglas de trabajo»): nada de eso se podía mover. `tools/build.py` calcula la raíz como el padre de su propia carpeta (`os.path.dirname(aqui)`) para leer `app.json` y `data/*.data.json`, leer las plantillas de `src/` y escribir los tres generados en la raíz. `tools/extraer.js` y `tools/extraer-js.js` ya no asumen que la tarjeta de origen y la salida comparten carpeta con el script: escriben `<región>.data.json` junto a la tarjeta que reciben (`path.dirname(archivo)`), así que siguen funcionando igual se invoquen desde donde se invoquen. Los cinco `data/tarjeta_<región>.js` pasan de `require('./plantilla_tarjetas')` a `require('../tools/plantilla_tarjetas')`; es el único cambio de código en esos archivos (aparte del `git mv`) y no afecta al docx: los 5 se comprobaron byte a byte contra `word/document.xml` de antes de mover nada, idénticos. Las pruebas en `test/` usan `path.join(__dirname, '..', …)` para llegar a la raíz, `data/` o `src/`; `test/test-fuente.js` además cambia sus `require('./extraer-js')`/`require('./datos')` a `require('../tools/…')`. `package.json` actualiza sus cinco scripts a las rutas nuevas. `npm test` sigue en verde (14 pruebas) y `npm run build` no cambia ni un byte de `index.html`/`manifest.webmanifest`/`service-worker.js`: la reorganización es invisible para lo publicado.
8. ~~Enlaces de descarga a la ficha de primera visita, la guía rápida y los formularios previos.~~ **Hecho.** Motivo: las anotaciones de la tarjeta y de la ficha (p. ej. «criterio fijo en la guía rápida») remiten a documentos en papel que, sin la app, nadie puede consultar. Documentos propios de la SPA (decisión 2), no CONTENIDO clínico: preguntas de un formulario previo pueden citar hallazgos de la guía, pero el texto en sí es de la SPA, igual que `ENLACES` o `FICHAS`.
   Un único punto de acceso general (se descartó un enlace junto a cada anotación: son pocas menciones y habría siete puntos de mantenimiento por siete anotaciones distintas para el mismo documento). Enlace «Formularios, ficha y guía rápida para descargar» al final del selector del inicio, tras la cita (`enlaceSecundario`, clase `.enlace` propia — ni `.nav` ni `.doc` — para no parecer una séptima región: con el mismo aspecto de tarjeta que las regiones, un primer intento se leía como una más de la lista; una línea de texto separada por un borde, sin chevron de navegación, se distingue de un vistazo). Lleva a una pantalla nueva, «Documentos» (`pDocumentos`, registrada en `PANTALLAS`), donde los ocho enlaces de descarga sí son el contenido principal y mantienen el aspecto de tarjeta (`docLink`, `<a download>` a `descargas/*.docx`, clase `.doc`). `render()` gana un caso para `documentos` en el título de pestaña y en `#regionCab` (pantalla alcanzable sin ninguna región elegida, `D` es `null` ahí).
   Los `.docx` se descargan tal cual (no PDF): así no hay paso de conversión que pueda desincronizarse del contenido, aunque en el móvil dependen de tener una app de Office instalada — pendiente clínico/de producto, no de código (decisión del usuario).
   Fuente en el repo, mismo patrón que las tarjetas: `data/formulario_<región>.js` (las seis) + `tools/plantilla_formularios.js` (librería compartida: cara 1 común + helpers de cara 2) generan `descargas/formulario_previo_<región>.docx`; `data/ficha_primera_visita.js` (script suelto, sin plantilla compartida, más `data/body_chart.jpg`) genera `descargas/ficha_primera_visita.docx`. `guia_rapida.docx` no tiene generador (no se aportó su fuente): se comitea tal cual en `descargas/`, igual que los iconos de la PWA. `npm run formularios` (nuevo, mismo patrón `OUT_DIR` que `npm run tarjetas`) regenera los siete; a diferencia de `index.html`, no hay test de que `descargas/` esté al día tras un cambio de fuente (los `.docx` no son reproducibles byte a byte de una ejecución a otra: la librería `docx` fecha el `core.xml` del zip), así que toca acordarse de correr `npm run formularios` y comitear el resultado a mano cuando cambie una fuente.
   `test-descargas.js` nuevo (`npm test`): la entrada a documentos no se cuenta entre las seis regiones del selector, la pantalla trae los ocho enlaces con `download` y rutas relativas, y cada archivo enlazado existe de verdad en el repo.

## Pendientes clínicos (decide el usuario; no tocar sin preguntar)
- ~~La tarjeta de hombro dice «Máximo 2–3 tests» y la Ficha dice «Dos como máximo». Sin resolver.~~ **Resuelto:** se queda «Máximo 2–3 tests» (ya era el texto de `SINDROMES.aviso` en `tarjeta_hombro.js`); si la Ficha de primera visita en papel dice otra cosa, es la Ficha la que habría que corregir, no la tarjeta.
- ~~② propone dinamómetro; no está en la lista de recursos de la consulta.~~ **Resuelto:** se sugiere, no se exige. Las cuatro filas de SINDROMES que lo mencionan (SAPS, Inestabilidad GH, SLAP, Rotura del manguito) dicen ahora «con dinamómetro si se dispone»; SAPS y Rotura del manguito ya tenían alternativa (grados de elevación activa), Inestabilidad GH y SLAP no la tenían y se dejan solo con el matiz «si se dispone» (no se ha inventado una alternativa que no esté en la guía).
- La agrupación de la tabla de rigidez (pasiva limitada / no limitada) es interpretación propia.
- ~~Cuestionarios validados (SPADI, DASH, ASES, SST, Constant): la guía los cita en el apartado 6, van aparte de los tres números. No se incluyen; no reproducir ítems ni inventar puntuaciones.~~ **Resuelto (parcial):** ahora se mencionan los nombres en `TITULOS.pieB` de `tarjeta_hombro.js` (pie de la cara B en la SPA y el docx). Sigue sin reproducirse ningún ítem ni puntuación: eso sigue fuera de alcance.

## Reglas de trabajo
- **`.gitignore` con `node_modules/` y `salida/`** (ya está). El repo se sube a mano (sin carpetas ni archivos ocultos) y una sesión en la nube podría commitear `node_modules`.
- Cambios de contenido clínico: primero en la tarjeta o la guía, después reflejarlos aquí.
- Antes de dar algo por hecho: `npm test` (ya regenera `index.html` y falla si no coincidía con el del repo).
- Si cambia `data/formulario_<región>.js`, `data/ficha_primera_visita.js`, `data/body_chart.jpg` o `tools/plantilla_formularios.js`: `npm run formularios` y comitear los `.docx` de `descargas/` regenerados. A diferencia de `index.html`, `npm test` no lo comprueba solo (los `.docx` no son reproducibles byte a byte, ver tarea 8): toca acordarse a mano.
- Publicar en GitHub Pages **desde una rama, sin GitHub Actions** (los tokens de las sesiones en la nube pueden no poder empujar archivos de workflow).
- Sin datos de pacientes en el repo, en pruebas ni en ejemplos.
